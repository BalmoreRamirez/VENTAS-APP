const models = {
    CategoryModel: require('../models/categorias'),
    ProductoModel: require('../models/productos'),
    ProveedorModel: require('../models/proveedores'),
};

module.exports = models;
